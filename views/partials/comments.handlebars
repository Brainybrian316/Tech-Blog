{{!-- 
<div class="container">
    <div class="row">
        <div class="col-12">
            {{#each this}}
            <div class="card">
                {{user.username}} posted: {{content}} on {{formate_date created_at}}
            </div>
        </div>
        {{/each}}
    </div>
</div> --}}

<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>